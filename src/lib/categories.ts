/**
 * Allowed collectible categories
 */
export type Categories = "all" | "warframes"

/**
 * An array of allowed collectible categories
 */
export const categories: ReadonlyArray<Categories> = ["all", "warframes"]
